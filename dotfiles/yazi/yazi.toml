# Matches Ghostty ayu-dark. Full-screen read is `i` (keymap.toml).
[mgr]
ratio = [1, 3, 4]
linemode = "mtime"
sort_by = "mtime"
sort_reverse = true
sort_dir_first = true

[[plugin.prepend_previewers]]
url = "*.md"
run = 'piper -- CLICOLOR_FORCE=1 glow -w=$w -s=dark "$1"'
